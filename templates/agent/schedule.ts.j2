import { defineSchedule } from "eve/schedules";

export default defineSchedule({
  cron: "{{ schedule.schedule }}",
  markdown: "Run scheduled task {{ schedule.name }} and report only material findings.",
});
