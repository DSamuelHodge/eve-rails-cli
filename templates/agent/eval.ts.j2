import { defineEval } from "eve/evals";

export default defineEval({
  description: "{{ eval.name }} generated placeholder eval.",
  async test(t) {
    await t.send("Run the {{ eval.name }} smoke check.");
    t.succeeded();
  },
});
